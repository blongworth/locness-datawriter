# AWS Configuration
AWS_ACCESS_KEY_ID=your_aws_access_key_id
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key
AWS_REGION=us-east-1
DYNAMODB_TABLE_NAME=locness-underway-summary

# Google Drive Configuration
GOOGLE_CREDENTIALS_FILE=./credentials.json
# For Google Shared Drive (recommended for service accounts):
GOOGLE_SHARED_DRIVE_ID=your_shared_drive_id_here
# Optional: specific folder within the shared drive
GOOGLE_DRIVE_FOLDER_ID=your_folder_id_here

# CSV Configuration
CSV_NAME_PREFIX=locness_data

# Railway Configuration (optional)
PORT=8080
